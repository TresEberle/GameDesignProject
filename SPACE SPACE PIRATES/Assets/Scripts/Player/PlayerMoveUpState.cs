using UnityEngine;

public class PlayerMoveUpState : PlayerMoveState
{
    public PlayerMoveUpState(Player player, StateMachine stateMachine, string stateName) : base(player, stateMachine, stateName)
    {
    }



    // Update is called once per frame
    void Update()
    {
        base.Update();






    }
}
