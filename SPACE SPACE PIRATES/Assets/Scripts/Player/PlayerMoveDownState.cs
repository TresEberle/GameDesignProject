using UnityEngine;

public class PlayerMoveDownState : PlayerMoveState
{
    public PlayerMoveDownState(Player player, StateMachine stateMachine, string stateName) : base(player, stateMachine, stateName)
    {
    }


    // Update is called once per frame
    void Update()
    {
        base.Update();


 

    }
}
